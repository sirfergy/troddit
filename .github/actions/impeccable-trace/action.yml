name: Log Impeccable native invocations
description: Collect bounded native invocation metadata after the review job.
inputs:
  sha256:
    description: Expected SHA-256 of the already installed native engine.
    required: true
runs:
  using: node24
  main: main.cjs
  post: post.cjs
  post-if: always()
