version: '3'

services:
  app: 
    image: troddit
    build:
      dockerfile: Dockerfile
    ports: 
      - 3000:3000
      - 4321:4321
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - COPILOT_CLI_PORT=4321
